# quick t2i collapse diagnostic: gen124 step5000

- Timestamp: `2026-05-06T18:21:14+08:00`
- Status: `completed`
- Purpose: quick root-cause read on text-to-image collapse, not a training experiment.
- Code SHA: `2c7b78f`
- Config: `configs/flm_words_joint_long_rope_p8_gen124_gpu80.yaml`
- Checkpoint: `/fangxueji/Projects/PG/uniindex/worktrees/gpu-fd41e0b/runs/20260506T033603Z-stage2-continue/checkpoints/stage2_step005000.pt`
- Remote output: `/fangxueji/Projects/PG/uniindex/worktrees/diag_t2i_collapse_gen124_step5000/logs/diagnostics/t2i_collapse_gen124_step5000_quick`
- Local output: `logs/diagnostics/t2i_collapse_gen124_step5000_quick`
- Runtime: `311.548s`

## Setup

- Modes: `correct`, `empty`
- Seeds per label: `8`
- Sampling: `NFE=256`, `temperature=0.7`
- Recorded steps: `0, 1, 2, 4, 8, 16, 32, 64, 128, 192, 256`, plus final logits argmax at `257`
- Bank samples: `5000`
- No training, no model/objective change, no Qwen/causal/sequence fallback.

## Main Read

Collapse is not present at initialization. It develops late in the denoising trajectory and becomes extreme at the final logits argmax.

- Correct prompt collapse mass on labels `9/1/7`:
  - step `0`: `0.1875`
  - step `128`: `0.4875`
  - step `256`: `0.775`
  - final argmax `257`: `1.0`
- Empty prompt collapse mass on labels `9/1/7`:
  - step `0`: `0.1875`
  - step `128`: `0.3625`
  - step `256`: `0.8875`
  - final argmax `257`: `1.0`

Prompt has an effect, but it is too weak to prevent collapse.

- Correct final nearest-label distribution: `{1: 0.2625, 7: 0.275, 9: 0.4625}`
- Empty final nearest-label distribution: `{1: 0.8625, 7: 0.0125, 9: 0.125}`
- Final `correct` prompt match / target match: `0.3`
- Final `empty` target match: `0.0875`
- Correct-vs-empty final distribution L1: `1.2`

## Entropy / Concentration

Entropy drops immediately by step `2`, then barely changes.

- Correct entropy: step `1` `7.918307`, step `2` `3.836213`, final `3.433593`
- Empty entropy: step `1` `7.900037`, step `2` `3.964612`, final `3.580635`
- Correct top-token concentration: step `2` `0.446841`, final `0.477958`
- Empty top-token concentration: step `2` `0.43456`, final `0.465691`

This suggests local token confidence forms very early, but semantic nearest-label collapse happens late.

## Position Note

The earliest high-confidence positions are mostly edge/background-like image-token positions, for example positions `0-5`, `31`, and `192`. Their early confidence is likely background/layout certainty, not semantic digit selection.

## Visual Note

Final decoded grids match the numeric conclusion:

- Correct prompts show some prompt control for easy classes like `one` and `seven`.
- `zero`, `two`, `four`, and `nine` still visibly drift toward `9/1/7`.
- Empty prompts mostly drift toward `one`, independent of target class.

## Conclusion

The failure mode is best described as **late denoise / final argmax attractor collapse**, not initialization collapse. Text conditioning is present, because correct and empty prompts end in very different distributions, but it is not strong enough to steer the late image trajectory away from a few attractors.

## Next

Do not launch more training yet. The next cheap diagnostic should compare the image-token state at step `256` before final logits argmax with sampled/non-argmax finalization or trajectory projection variants, to isolate whether the biggest damage is the final logits readout or the last denoise segment.
