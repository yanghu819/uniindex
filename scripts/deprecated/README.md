# Deprecated Scripts

These scripts are kept for historical reproducibility and should not be used as active entrypoints.

Use the unified FLM/RoPE path under `src/uniindex/` plus `scripts/continue_stage2.py`, `scripts/eval_image_to_text_tokens.py`, `scripts/eval_text_to_image_tokens.py`, `scripts/sweep_text_to_image_decoded.py`, and `scripts/diagnose_t2i_collapse.py`.

## Folders

- `qwen_fallback/`: abandoned fallback attempts. They are not part of the current unified FLM mainline.
